@model WebApplication.Models.LoginViewModel
@{
    ViewData["Title"] = "Login Page Building";
}

<div class="login-dark">
    <div class="form-group">@Model.Email</div>
    <div class="form-group">@Model.Password</div>
</div>